# Claims Ledger

| Claim | Status | Evidence Required | Reviewer Risk Control |
|---|---|---|---|
| `J1-Frontier` is a benchmark contribution, not another classifier paper. | Primary | Causal-prefix contract, comparator lock, split report, provenance chain, frontier package. | Keep the paper framed around benchmark objects and negative-control universal-winner testing. |
| No fixed operating point universally dominates the matched plain confidence-threshold baseline across the amputee-relevant primary families. | Primary | `j1_scorecard.md`, `primary_pairwise.csv`, matched-budget pairwise outputs, claim-safe family hierarchy. | Do not dilute confirmatory scope with `j1_subject_logo` or boundary families. |
| Matched plain confidence thresholding is the decisive comparator and must be matched within claim-bearing units. | Primary | `j1_confidence_gate_match_summary.csv` plus within-unit budget matching outputs in the frontier package. | Do not rely on pooled budget matching alone. |
| `j1_amputee_loso` and `j1_mixed_to_amputee` are the primary in-dataset evidence families. | Primary | Split report plus claim config and family-specific frontier outputs. | Treat `j1_able_to_amputee` as boundary and `j1_subject_logo` as descriptive only. |
| The benchmark should report frontier gain, universal-`tau` regret, and non-dominated structure rather than winner-take-all averages. | Primary | Pareto frontier outputs, regret table, overlap summary, instability metrics. | Do not claim frontier structure without emitting the actual package artifacts. |
| Safety/timing must be reported as a first-class tradeoff, not inferred from active macro-F1 alone. | Primary | Earliest-safe and stable-safe summaries plus safety/timing frontier figure. | Timing claims stay offline-only and must be supported by non-degenerate prefix resolution. |
| Modality-time value is a core future-facing contribution only if J1 emits explicit phase-resolved ablations. | Boundary | Lead/lag modality-time value report and pairwise outputs. | Do not claim modality-time decomposition from the current minimal readout alone. |
| External datasets support robustness claims, not the core J1 causal-prefix benchmark claim. | Secondary | Hyser, CEMHSEY, and GRABMyo support reports. | Do not use external datasets to retroactively rescue a failed J1 confirmatory claim. |
| Any model-search or challenger story must be registry-based and frozen before final readout. | Boundary | Candidate registry, comparator lock, promotion report, challenger manifest. | No open-ended Cartesian sweep, no post hoc tau densification, no family-specific model shopping. |
| This is not a clinical, online, embodiment, workload, trust, or haptic validation study. | Boundary | Explicit language in abstract, discussion, and claim reports. | Reject translational wording unless future direct evidence exists. |

